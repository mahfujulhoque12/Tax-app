import Login from '@/components/Login/Login';
import React from 'react';

const signin = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default signin;